--- a/supabase/migrations/20260318133000_admin_orders_performance.sql
+++ b/supabase/migrations/20260318133000_admin_orders_performance.sql
@@ -14,20 +14,20 @@ create or replace function public.admin_fetch_orders(
 )
 returns table (
   id uuid,
-  order_id text,
+  order_number text,
   user_id uuid,
   customer_name text,
   customer_phone text,
-  shipping_address text,
-  shipping_city text,
-  shipping_state text,
-  shipping_pincode text,
-  items jsonb,
-  subtotal numeric,
-  tax numeric,
-  shipping numeric,
-  total numeric,
+  shipping_address jsonb,
+  customer_email text,
+  subtotal_amount integer,
+  discount_amount integer,
+  tax_amount integer,
+  shipping_amount integer,
+  grand_total_amount integer,
+  item_count integer,
   payment_method text,
+  order_notes text,
   status text,
   estimated_delivery text,
   created_at timestamptz,
@@ -38,20 +38,22 @@ security invoker
 set search_path = public
 as $$
   select
     o.id,
-    COALESCE(o.order_id, o.order_number) AS order_id,
+    o.order_number,
     o.user_id,
     o.customer_name,
     o.customer_phone,
     o.shipping_address,
-    o.shipping_city,
-    o.shipping_state,
-    o.shipping_pincode,
-    o.items,
-    o.subtotal,
-    o.tax,
-    o.shipping,
-    o.total,
+    o.customer_email,
+    o.subtotal_amount,
+    o.discount_amount,
+    o.tax_amount,
+    o.shipping_amount,
+    o.grand_total_amount,
+    o.item_count,
     o.payment_method,
+    o.order_notes,
     o.status,
-    o.estimated_delivery,
+    COALESCE(o.estimated_delivery_at::text, ''),
     o.created_at,
     o.updated_at,
@@ -61,8 +63,8 @@ as $$
     (p_status is null or o.status = p_status)
     and (
       p_search is null
-      or COALESCE(o.order_id, o.order_number) ilike '%' || p_search || '%'
+      or o.order_number ilike '%' || p_search || '%'
       or o.customer_name ilike '%' || p_search || '%'
     )
   order by o.created_at desc
   limit greatest(coalesce(p_limit, 20), 1)
   offset greatest(coalesce(p_offset, 0), 0);
